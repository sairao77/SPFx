import {
  BasePrimaryTextCardView,
  IPrimaryTextCardParameters,
  IExternalLinkCardAction,
  IQuickViewCardAction,
  ICardButton
} from '@microsoft/sp-adaptive-card-extension-base';
import * as strings from 'MyTeamsAdaptiveCardExtensionStrings';
import { IMyTeamsAdaptiveCardExtensionProps, IMyTeamsAdaptiveCardExtensionState, QUICK_VIEW_REGISTRY_ID } from '../MyTeamsAdaptiveCardExtension';

export class CardView extends BasePrimaryTextCardView<IMyTeamsAdaptiveCardExtensionProps, IMyTeamsAdaptiveCardExtensionState> {
  public get cardButtons(): [ICardButton] | [ICardButton, ICardButton] | undefined {
    return [
      {
        title: strings.QuickViewButton,
        action: {
          type: 'QuickView',
          parameters: {
            view: QUICK_VIEW_REGISTRY_ID
          }
        }
      }
    ];
  }

  public get data(): IPrimaryTextCardParameters {
    return {
      primaryText: 'You are part of '+ this.state.myTeamDetails.myTeamCount +' Teams',
      description: strings.Description,
      title: this.properties.title
    };
  }

  public get onCardSelection(): IQuickViewCardAction | IExternalLinkCardAction | undefined {
    return {
      type: 'ExternalLink',
      parameters: {
        target: 'https://www.bing.com'
      }
    };
  }
}
